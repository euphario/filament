[package]
name = "bpi-r4-kernel"
version = "0.1.0"
edition = "2021"
authors = ["Your Name"]
description = "Bare-metal kernel for Banana Pi BPI-R4 (MT7988A)"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
codegen-units = 1
