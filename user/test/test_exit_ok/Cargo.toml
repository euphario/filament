[package]
name = "test_exit_ok"
version = "0.1.0"
edition = "2021"

[dependencies]
userlib = { path = "../../userlib" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
