[package]
name = "consoled"
version = "0.1.0"
edition = "2021"

[dependencies]
libsys = { path = "../../libsys" }
libos = { path = "../../libos" }
libf = { path = "../../libf" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
