[package]
name = "partd"
version = "0.1.0"
edition = "2024"
description = "Partition table driver"

[dependencies]
libsys = { path = "../../libsys" }
libos = { path = "../../libos" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
