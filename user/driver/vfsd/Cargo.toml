[package]
name = "vfsd"
version = "0.1.0"
edition = "2024"
description = "Virtual filesystem daemon"

[dependencies]
libsys = { path = "../../libsys" }
libos = { path = "../../libos" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
