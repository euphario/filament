[package]
name = "sshd"
version = "0.1.0"
edition = "2021"

[dependencies]
userlib = { path = "../../userlib" }
libf = { path = "../../libf" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
