[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Build tasks for BPI-R4 kernel"

# This is a host tool, not target code
[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
